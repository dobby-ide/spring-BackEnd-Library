#shows what sql is generated
spring.jpa.show-sql=true
#format Sql in console
spring.jpa.properties.hibernate.format_sql=true
#show bind values
logging.level.org.hibernate.orm.jdbc.bind=trace
#shows the address of the initialized inmemory h2 db
spring.h2.console.enabled=true

spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
spring.session.timeout.seconds=900

#spring.datasource.initialization-mode=always
#spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost:5432}/${DB_NAME:restdb}
spring.datasource.username=${DB_USER:fabio}
spring.datasource.password=${DB_PASSWORD:PItagora78}
spring.jpa.database=POSTGRESQL
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5


